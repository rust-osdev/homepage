# Configuration for https://github.com/crate-ci/typos

[files]
extend-exclude = [
    # List of files (without leading ./)
]

[default]
extend-ignore-identifiers-re = [
    "^BARs$",
    "[@Programatic]",
]

[default.extend-words]
HDA = "HDA"  # Intel High Definition Audio (not a typo)

[default.extend-identifiers]
